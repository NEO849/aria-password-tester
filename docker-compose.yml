# Startet den ARIA Password Tester hinter einem einfachen Container
services:
  aria-password-tester:
    build: .
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${HOST_PORT:-8000}:8000"
    env_file:
      - .env
    restart: unless-stopped
